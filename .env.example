# Database Configuration (PostgreSQL for production)
DATABASE_URL="postgresql://username:password@localhost:5432/residentone_db?schema=public"
SHADOW_DATABASE_URL="postgresql://username:password@localhost:5432/residentone_shadow?schema=public"

# Environment
NODE_ENV="development"

# Authentication
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secure-secret-key-change-in-production-minimum-32-characters"
JWT_SECRET="your-jwt-secret-for-client-approval-tokens"

# 🚀 ENTERPRISE CLOUD STORAGE (Dropbox) - Primary Solution
# Configure these for Dropbox cloud file storage:
DROPBOX_ACCESS_TOKEN="your-dropbox-access-token"
DROPBOX_APP_KEY="your-dropbox-app-key"
DROPBOX_APP_SECRET="your-dropbox-app-secret"

# Alternative: AWS S3 Storage (Optional - if you prefer AWS over Dropbox)
# AWS_REGION="us-east-1"
# AWS_ACCESS_KEY_ID="your-aws-access-key"
# AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
# AWS_S3_BUCKET="your-interior-design-files-bucket"
# AWS_CLOUDFRONT_URL="https://your-cloudfront-domain.com"  # Optional CDN
# Email Configuration - Mailgun (Production) - Recommended
MAILGUN_API_KEY="your-mailgun-api-key"
MAILGUN_DOMAIN="yourdomain.com"
MAILGUN_URL="https://api.mailgun.net"
EMAIL_FROM="ResidentOne <noreply@yourdomain.com>"

# Email Fallback - SMTP (Alternative)
# EMAIL_HOST="smtp.gmail.com"
# EMAIL_PORT="587"
# EMAIL_USER="your-email@gmail.com"
# EMAIL_PASSWORD="your-app-password"

# Email Development - MailHog (Local testing)
# EMAIL_HOST="localhost"
# EMAIL_PORT="1025"

# Application Configuration
APP_NAME="ResidentOne"
APP_URL="http://localhost:3000"
COMPANY_NAME="Your Interior Design Studio"

#
# 💼 ENTERPRISE DEPLOYMENT CHECKLIST:
#
# For a professional $100k interior design solution:
#
# ✅ FILE STORAGE:
#    Configure Dropbox above for secure, scalable file storage
#    Files are automatically organized: /interior-design/{projectId}/rooms/{roomId}/sections/{sectionId}/
#    Alternative: AWS S3 with CloudFront CDN for enterprise-scale deployments
#
# ✅ DATABASE:
#    Use PostgreSQL on AWS RDS or similar managed service for production
#    Current: SQLite (dev only)
#
# ✅ SECURITY:
#    - Enable HTTPS with SSL certificates
#    - Use strong, unique NEXTAUTH_SECRET
#    - Configure CORS and rate limiting
#    - Rotate Dropbox access tokens regularly
#    - Enable Dropbox team features for business accounts
#
# ✅ MONITORING:
#    - Set up error tracking (Sentry)
#    - Configure performance monitoring
#    - Enable logging and analytics
#
